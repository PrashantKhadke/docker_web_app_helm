apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docker_web_app_helm.fullname" . }}-test-connection"
  labels:
    {{- include "docker_web_app_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docker_web_app_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
